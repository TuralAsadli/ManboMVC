@using MambaMVC1.Models;
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<Employee>

<a asp-action="create"><button type="button" class="btn btn-primary">Create</button></a>
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Inverse table</h4>
            <p class="card-description">
                Add class <code>.table-dark</code>
            </p>
            <div class="table-responsive pt-3">
                <table class="table table-dark">
                    <thead>
                        <tr>
                            <th>
                                #
                            </th>
                            <th>
                                First name
                            </th>
                            <th>
                                LastName
                            </th>
                            <th>
                                Image
                            </th>
                            <th>
                                Position
                            </th>
                            <th>
                                Delete
                            </th>
                            <th>
                                Update
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @item.FirstName
                                </td>
                                <td>
                                    @item.LastName
                                </td>
                                <td>
                                    <img src="~/assets/img/team/@item.ImgName">
                                </td>
                                <td>
                                    @item.Position.PositionName
                                </td>
                                <td>
                                    <a asp-action="Delete" asp-route-id="@item.Id"><button asp-action="Delete" type="button" class="btn btn-danger">Delete</button></a>
                                </td>
                                <td>
                                    <a asp-action="Update" asp-route-id="@item.Id"><button asp-action="Update" type="button" class="btn btn-warning">Update</button></a>
                                </td>
                            </tr>
                        }
                        
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>